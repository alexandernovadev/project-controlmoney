/**
 * Configuración para Google Sign-In.
 *
 * Obtén el Web Client ID desde:
 * Firebase Console > Authentication > Sign-in method > Google > Web SDK configuration
 *
 * También debes agregar la URI de redirección en Google Cloud Console:
 * APIs & Services > Credentials > Tu OAuth 2.0 Client ID > Authorized redirect URIs
 * Para Expo Go usa la URI que muestra makeRedirectUri() al iniciar sesión.
 */
export const GOOGLE_WEB_CLIENT_ID =
  '441923672949-xxxxxxxxxxxxx.apps.googleusercontent.com';
